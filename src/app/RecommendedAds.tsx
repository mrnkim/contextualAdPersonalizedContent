import React from 'react'

interface RecommendedAdsProps {
  hashtags: string[];
}

const RecommendedAds = ({ hashtags }: RecommendedAdsProps) => {
    //TODO: Add generageGist if hashtags are empty
  return (
    <div>

    </div>
  )
}

export default RecommendedAds
